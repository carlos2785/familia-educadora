import React from 'react';
import ReactDOM from 'react-dom/client';// Con DOM se especifica que es una aplicación desde la web
import { App } from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App/>);